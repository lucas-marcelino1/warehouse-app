<h1> Galpões & Estoque </h1>

<% if @warehouses.any? %>

    <% @warehouses.each do |w| %>

        Nome: <%= w.name %>
        Código: <%= w.cod %>
        Cidade: <%= w.city %>
        Área: <%= w.area %> m²

    <% end %>

<% else %>

   <p> Não há galpões cadastrados </p>
   
<% end %>